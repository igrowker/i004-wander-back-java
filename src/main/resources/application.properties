# Application name
spring.application.name=Wander

# MongoDB configuration
spring.data.mongodb.uri=mongodb+srv://<user>:<password>@wanderDB.j7bl8.mongodb.net/wanderDB?retryWrites=true&w=majority&appName=wanderDB

# Set log level to DEBUG to facilitate debugging (optional, recommended during development, switch to INFO when the development ends)
logging.level.org.springframework.data.mongodb=DEBUG

# Spring Security
jwt.secret=${JWT_SECRET_KEY}
jwt.expiration=3600000

spring.config.import=optional:file:.env[.properties]